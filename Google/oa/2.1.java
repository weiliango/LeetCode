class Solution {    public int lengthLongestPath(String input) {        int max_len = 0;        Stack<Integer> stack = new Stack<>();        for(String s: input.split("\n")) {            int lev = s.lastIndexOf("\t") + 1;            int len = 0;            while(stack.size() > lev) stack.pop();            len = (stack.isEmpty() ? 0 : stack.peek()) + s.length() - lev + 1;            stack.push(len);            if(s.contains("."))                max_len = Math.max(max_len, len - 1);            // if(s.contains(".jpg") || s.contains(".gif"))                // max_len = Math.max(max_len, len - 1);        }        return max_len;    }} // sum of jpg and gif file pathsclass Solution {    public int sumOfImgFilePaths(String path) {        int sum = 0;        Stack<Integer> stack = new Stack<>();        stack.push(0);        for(String s: path.split("\n")) {            int lev = s.lastIndexOf("\t") + 1;            while(stack.size() > lev) stack.pop();            int len = stack.peek() + s.length() - lev + 1;            stack.push(len);            if(s.contains("jpg") || s.contains("gif")) {                sum += len - 1;            }        }        return sum;    }}